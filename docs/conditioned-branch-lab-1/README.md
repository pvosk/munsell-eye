# Sixteen destination-branching puzzles

This is the overdue playable selection from `conditioned-branch-search-1` and `conditioned-branch-search-2`, not the fresh region-versus-chain comparison. Ten cases were already visible in Atlas; six contrasting cases have been added from those same older research archives. All sixteen are newly playable in this collection.

Select **New · 16 destination branching puzzles** in the lab. Pick or replay any hole; the previous four branching setups and all older reviews remain available. Twelve cases have shortest-found three-leg solutions; four intentional two-leg contrasts add a shorter dark drop, two chromatic examples, and a five-paint/raw-umber lift. This is not a claim that every case is difficult. Par remains 3, unchanged and not calibrated to this selection.

The same destination appears in Secondaries, Maroon/Lemon/Green, and Sienna/Orange/Prussian so that palette-dependent corrections can be compared directly. Other targets vary in lightness and chroma. Requested styles remain distinct from observed route traits: Cobalt Ember's dark target is labeled interior, not a demonstrated drop finish. Style availability does not imply necessity or player enjoyment.

## Validation and provenance

- Immutable IDs: `conditioned-lab-1-0` through `conditioned-lab-1-15`; exact source IDs, original pigment snapshots, starts and targets are retained.
- Fresh ordered-fraction shortcut checks: 384 samples and 20 restarts per order, through one fewer leg than the previous raw minimum. All sixteen retained their previous shortest-found minimum in this finite search. Individual checks and their seeds are archived alongside `selection.json`.
- Featured routes are proportion-supported, meaningful efficient examples and have executable finite hold/release controls. Every displayed alternative is replayed through current normalized controls and rejected if an earlier release already lands.
- Route geometry is rechecked at 192 samples per leg. Finish windows contain the actual selected release, not an unrelated accepted interval.
- Tests cover every saved example and alternative, historical bank indexing, review-event validation, collection wraparound, unlocked picker rendering, and colored route review rendering. These are data/render checks, not device interaction QA.
- Pigment strength, normalized mass, live scoring tolerance, par, input policy, camera and sound are unchanged. The Sites publication preserves the existing public audience.

Rebuild with `node --import tsx scripts/build-conditioned-lab.ts`. Published output is guarded against mutation. The new `search-region-comparison.ts` investigation is separate and cannot overwrite this collection.
